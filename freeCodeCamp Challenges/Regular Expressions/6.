let twinkleStar = "Twinkle, twinkle, little star";
let starRegex = /twinkle/gi; // where g means - repaeat the pattern
//i means ignore the case
let result = twinkleStar.match(starRegex); // Change this line
